DB_URI=mongodb://localhost:27017/
DB_NAME=payli
NODE_ENV=development
PORT=3000
PAYMENT_GATEWAY_URI=https://pay.payli.to
SERVICE_FEE=
ALCHEMY_API_KEY=

# Admin panel. The two shared secrets are exchanged for a JWT at POST /admin/login;
# ADMIN_JWT_SECRET signs that JWT. Use long random values (e.g. `openssl rand -hex 32`).
# When any of the three is unset, /admin/* endpoints return 503 (the rest of the
# API is unaffected). ADMIN_TOKEN_TTL_SEC is optional (default 43200 = 12h).
ADMIN_ID=
ADMIN_SECRET=
ADMIN_JWT_SECRET=
ADMIN_TOKEN_TTL_SEC=43200

# Optional. Absolute path to the main log file. When set, the app ALSO writes a
# FATAL-only sibling next to it (e.g. /var/log/payli/app.log -> app.fatal.log) so
# crashes are easy to find. Leave unset in local dev to log to stdout only.
LOG_FILE_PATH=

# Used by docker compose (not the app directly) to build LOG_FILE_PATH and pick
# the host log directory. Defaults if unset: LOG_FILENAME=app.log, LOG_DIRNAME=./storage
LOG_FILENAME=app.log
LOG_DIRNAME=./storage
